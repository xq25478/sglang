# DSV4 LayerSplit 迁移方案：wwm → moe_gate_fused_0516-DSpark

## 背景

- **目标分支**：`origin/moe_gate_fused_0516-DSpark`（tip `968f9d9726`），只有 GLM LayerSplit
- **来源分支**：`origin/moe-gate-fused-layersplit-wwm`（tip `de327e4392`），有完整 DSV4 LayerSplit
- **merge-base**：`9a6f8e5992`（2026-07-06），两分支从此分叉后各自大量演进
- **策略**：squash port（取 wwm tip 最终文件状态，手动集成到 0516-DSpark），不做逐 commit cherry-pick（会产 75+ 冲突标记 + 架构级 add/add 冲突）
- **GLM 兼容**：保留现有 GLM LayerSplit，新增 DSV4 LayerSplit，两套开关独立（`enable_dsa_cache_layer_split` / `enable_cp_cache_layer_split`）

## wwm LayerSplit commit 链（6 个，最终状态全部体现在 tip 文件中）

| # | commit | 作用 |
|---|--------|------|
| 1 | db9a27c3fd | 基础引入：cp_cache_layer_split 包 + DSV4 集成（38 文件 +4305/-209）|
| 2 | 2f103710eb | 加固 + MTP 支持（16 文件 +496/-229）|
| 3 | 6350c2fb27 | rebase 修复（2 行）|
| 4 | 3251ebcdd2 | env gate import 修复（2 行）|
| 5 | 4c016fcc46 | non-owner compressed page size |
| 6 | de327e4392 | remap sparse prefill cache |

## 执行步骤

### Phase 0：准备工作分支
- 在 `sglang-worktrees/` 下新建 worktree，基于 `moe_gate_fused_0516-DSpark` tip 创建分支 `codex/dsv4-layersplit-port`
- 确认 wwm 分支已 fetch

### Phase 1：Clean adds — cp_cache_layer_split 包 + 新测试（13 文件）
从 wwm tip 直接 checkout 以下文件（0516-DSpark 不存在，无冲突）：
- `python/sglang/srt/mem_cache/cp_cache_layer_split/`（8 文件：`__init__.py`, `broadcast.py`, `deepseek_v4_helpers.py`, `deepseek_v4_layout.py`, `deepseek_v4_pool.py`, `pool_base.py`, `staging.py`, `utils.py`）
- `python/sglang/srt/model_executor/model_runner_kv_cache_mixin.py`（新 mixin，但 DSpark 的 KV cache 构造在 `model_runner.py`，需 Phase 6 手动接线）
- 4 个新测试文件：`test_cp_cache_layer_split_deepseek_v4.py`, `test_cp_cache_layer_split_hicache_storage.py`, `test_cp_cache_layer_split_ownership.py`, `server_args/test_cp_cache_layer_split.py`
- `test/registered/unit/mem_cache/test_dsa_layer_shard_utils.py`（需手动合并 GLM+DSV4 用例，见 Phase 5）

**去重处理**：`cp_cache_layer_split/utils.py` 的 `get_layer_owner` / `get_layer_shard_range` 与 0516-DSpark 的 `layers/cp/utils.py` 完全相同——改为 `from sglang.srt.layers.cp.utils import get_layer_owner, get_layer_shard_range`，消除重复源。保留 wwm 独有的 `get_global_layer_shard_range` / `should_use_cp_cache_layer_split_pool` / `get_cp_cache_layer_shard_info`。

### Phase 2：Low-conflict 修改（~11 文件，≤1 冲突标记）
逐文件用 wwm tip 的 diff 手动应用，解 0-1 处冲突：
- `server_args.py`：+126 行（`enable_cp_cache_layer_split` 字段 + 3 个 validator）
- `arg_groups/deepseek_v4_hook.py`：+1 行
- `layers/attention/deepseek_v4_backend.py`：+36/-1 行（LayerSplit helpers）
- `layers/attention/dsv4/indexer.py`：+10/-2 行
- `disaggregation/decode.py`：+13 行
- `disaggregation/base/conn.py`：+1 行（`kv_data_layout` 字段）
- `managers/cache_controller.py`：+11/-2 行
- `disaggregation/common/utils.py`：+42 行
- `mem_cache/hicache_storage.py`：+7 行
- `mem_cache/deepseek_v4_memory_pool.py`：+85/-5 行
- `mem_cache/storage/mooncake_store/mooncake_store.py`：+29 行

### Phase 3：Medium-conflict 修改（~6 文件，2-6 冲突标记）
- `models/deepseek_v4.py`（2 标记）：LayerSplit guards + CP multi-stream
- `model_executor/pool_configurator.py`（3 标记）：LayerSplit pool 选择 + staging 组件
- `disaggregation/prefill.py`（3 标记）：LayerSplit draft 传输
- `disaggregation/utils.py`（6 标记）**最高风险**：0516-DSpark 已有更新的 DSV4 NextN draft-state 传输（`owner/semantic/tensor_dtypes` API），wwm 用 `data_layout` API。需手动融合：统一到 0516-DSpark 的 `append_state_component(owner=, semantic=)` API + 补 `is_cp_cache_layer_split_pool` 分支 + 补 `append_draft_kv_data` / `should_transfer_draft_cache`
- `test_disaggregation_wire.py`（5 标记）：合并两套测试用例
- `test_pool_configurator.py`（1 标记）

### Phase 4：High-conflict 修改（3 文件，7+ 标记）
- `mem_cache/hybrid_cache/hybrid_pool_assembler.py`（7 标记）：两分支都重度重构装配逻辑，需逐块手动合并
- `disaggregation/common/conn.py`（13 标记）：0516-DSpark 有 `state_dtypes/state_owners/state_semantics` 体系，wwm 引入 `kv_data_layout/state_data_layouts/require_descriptor_matched_transfer`。需在 dataclass 字段块和传输逻辑上融合
- `disaggregation/mooncake/conn.py`（9 标记）：0516-DSpark 几乎重写了 mooncake conn（327 行 diff），wwm 改了 103 行。需融合 `require_descriptor_matched_transfer` 字段 + transfer thread GPU 绑定（commit e17d72029b 的 `_set_mooncake_transfer_device`）

### Phase 5：dsa_cache_layer_split.py 手工合并（add/add 架构冲突）
以 wwm 版（`CpCacheLayerSplitPoolBase` + `BroadcastSlots`）为骨架，合并 GLM 触发路径：
- 保留 wwm 的多继承 `class LayerSplitDSATokenToKVPool(CpCacheLayerSplitPoolBase, DSATokenToKVPool)`
- 触发逻辑同时支持 `enable_dsa_cache_layer_split`（GLM）和 `enable_cp_cache_layer_split`（DSV4），两者互斥
- 统一 `index_buf_accessor` 导入路径（确认 0516-DSpark 实际导出位置）
- 保留 GLM 的 `is_glm_dsa_cache_layer_split_enabled` 路径 + DSV4 的 `should_use_cp_cache_layer_split_pool` 路径
- 合并 `test_dsa_layer_shard_utils.py` 测试用例

### Phase 6：Mixin 接线（隐性工作）
0516-DSpark 没有 `model_runner_kv_cache_mixin.py`，KV cache 构造在 `model_runner.py`。需要：
- 定位 `model_runner.py` 中 `init_kv_cache_configurator()` 或 `kv_cache_configurator.py` 的 pool 选择逻辑
- 把 mixin 的 LayerSplit pool 选择分支（`get_cp_cache_layer_shard_info` → `CpCacheLayerSplitDeepSeekV4TokenToKVPool` / `LayerSplitDSATokenToKVPool`）移植到对应位置
- 确保 GLM 的 `LayerSplitDSATokenToKVPool` 选择路径不被破坏

### Phase 7：验证
1. **语法检查**：`python3 -m py_compile` 所有改动文件
2. **容器内单测**（6.200.20.13 / zhangyu-sglang-latest）：
   - `test_cp_cache_layer_split_deepseek_v4.py` — DSV4 LayerSplit 核心测试
   - `test_cp_cache_layer_split_hicache_storage.py` / `test_cp_cache_layer_split_ownership.py`
   - `server_args/test_cp_cache_layer_split.py`
   - `test_dsa_layer_split_broadcast.py` — GLM 回归测试（确保不破坏）
   - `test_disaggregation_wire.py` — PD 传输测试
   - `test_pool_configurator.py`
3. **import 检查**：`python -c "from sglang.srt.mem_cache.cp_cache_layer_split import *"` 在容器内验证包可导入

## 风险评估
- **总改动量**：~32 文件，新增 ~2500 行，修改 ~1500 行
- **最高风险**：`disaggregation/utils.py`（两套 draft 传输 API 融合）、`dsa_cache_layer_split.py`（架构级合并）、`mooncake/conn.py`（重度分叉）
- **隐性风险**：mixin 接线（merge-tree 不报冲突但功能不生效）
- **GLM 回归风险**：合并 `dsa_cache_layer_split.py` 时可能破坏 GLM 路径，需跑 GLM 回归测试

## 预期产出
- 新分支 `codex/dsv4-layersplit-port`，基于 `moe_gate_fused_0516-DSpark`
- 单个 squash commit：`feat: port DSV4 CP LayerSplit from moe-gate-fused-layersplit-wwm`
- 所有 DSV4 LayerSplit 测试通过 + GLM 回归测试通过
- 可合入 `moe_gate_fused_0516-DSpark`